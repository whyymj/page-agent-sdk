# R2 数据操作审计结果(audit-sdk-integrity)

范围:src/core/tools/{dataOps,schemaUtils,jsonUtils,resources,toolError,dataSlotQuery}.ts + src/core/sdk/conflictManager.ts
基线:2.38.0 | 走查项:F2 数据写链 / F3 冲突闭环 / F8 受保护资源 / 假设 H4

## Findings(严重度降序)

P1|security|src/core/tools/dataOps.ts:732(及 382/572/597/629/937)|整体读/根级读只用浅投影,嵌套未声明字段泄露,白名单「隐藏未声明字段」承诺在整体读路径失效|read 整体/get_data/query/search/eval 根模式/diff 均调 projectBySchema(schemaUtils.ts:119-125 仅顶层 key 不递归);声明顶层 key 内深层未声明字段(如 config.apiKey)整体读全暴露,而子路径读(dataOps.ts:734-735)与 history 整体(552)都是深投影,同数据两口径|整体/根级路径改 projectBySchemaDeep(A4 推后项「投影深度不对称」上调实施)|selftest:bind 深层藏未声明字段,断言 read 整体/query/search 输出不含该字段
P1|flow-divergence|src/core/sdk/createChatSdk.ts:1549-1564|resetSession 是唯一不收口挂起冲突的会话生命周期路径(switchSession:1503 与 unmount:2147 均有 resolve)|resetSession 不调 conflictMgr.resolve 也不 abort 进行中流;ChatDialog 清空经 mountChatDialog.ts:53 onClear→resetSession 可在冲突挂起时触达→旧工具 Promise 永挂,且若之后被 resolve 会跨会话把写入落到新会话 bind|resetSession 头部加 conflictMgr.resolve('keep_external') 并评估联动 abort|e2e:构造挂起冲突→resetSession→断言 Promise 以 keep_external 收口、pendingConflict 归 null
P1|correctness|src/core/tools/schemaUtils.ts:42-44|isPathAllowed 的 ZodArray 分支把任意段当索引放行,非数字/负数索引写入静默成功但零落地|components.-1.x/components.abc.x 过白名单→setByPath 在 clone 数组挂非索引自有属性→zod4 数组校验忽略非索引属性→res.data 干净→写回=无变化,但返回「已 edit」成功消息并刷新 hash(对照 deleteByPath jsonUtils.ts:61 有 /^\d+$/ 校验,写路径没有)|array 段强制 /^\d+$/ 否则结构化报错|selftest:负索引/非数字索引/空段(write 四意图+edit_data)断言错误而非成功
P2|correctness|src/core/tools/resources.ts:295 + jsonUtils.ts:47|配了 data.resources 时 write({}) 空值触发 TypeError 而非结构化 SCHEMA_INVALID|write schema 全可选允许空调用;enforceSet→deepClone(undefined)=undefined→normalizeAndCheck 回填分支对 undefined 目标执行 setByPath→cur[k] 抛 TypeError;未配 resources 时同输入正常返 SCHEMA_INVALID|enforceSet 对非对象 normalized 早返回或 setByPath 加目标守卫|selftest:resources 配置 + write({})/write({value:undefined}) 断言结构化错误
P2|correctness|src/core/tools/jsonUtils.ts:174-181、18-24|merge op 的 value 非对象时 safeMerge 静默 early-return 且无错返回→假成功|write({patch:{op:'merge',jsonPath:'title',value:'x'}}) 目标对象检查通过但 src 非对象→什么都没合→schema 校验未变 clone 通过→成功消息;与 append 目标类型检查(报 PATCH_FAILED)不对称|applyPatchToClone merge 分支对非对象 value 返错误串|selftest:merge 标量/数组 value 断言 PATCH_FAILED
P2|correctness|src/core/tools/dataOps.ts:621-627|eval_script 子树模式 jsonPath 缺 isUnsafePath 检查,开放 schema 下误导成功|白名单 schema 由 isPathAllowed 兜住;allowKeys=null 时 jp='__proto__'→getByPath 守卫返 undefined→transform set 被 setByPath 守卫静默 no-op→成功消息+空快照入栈(无原型污染,但反馈失真且与 edit/write 的 PATH_UNSAFE 契约不一致)|子树入口补 isUnsafePath→PATH_UNSAFE|selftest:eval 子树 __proto__/constructor 路径断言 PATH_UNSAFE
P2|hang|src/core/sdk/conflictManager.ts:25-37 + createChatSdk.ts:2145-2160|冲突挂起无超时;unmount 只收口当前冲突不 abort 流,unmount 后新起的冲突永挂|abort 联动(createChatSdk.ts:1574-1579)/switchSession/unmount 三处收口已核实;但 send(invoke)无 signal→无 abort 收口链,headless+send 仅靠 conflict 事件可见性(events.ts 不过滤 conflict,types:69 有类型),无人 resolve 即永挂|conflict 挂起加可配超时(默认 keep_external)+ unmount 评估 abort 活跃流|e2e:send 路径冲突不 resolve→断言超时收口;conflict 事件进 onEvent 断言
P2|maintainability|src/core/tools/dataOps.ts:662-686|eval transform 整体替换是 fork 写链,未共用 commitSetToBind(F2①「单一真相源」不完全成立)|set 类三路径(set_data:405/write:877/draft_commit:990)共用 commitSetToBind ✓;edit 类四路径(443/864/655/642)共用 applyPatchesToBind ✓;唯 eval 整体替换独立实现 enforce→schema→snapshot→merge(顺序一致但 LEAF_BIND 检查位置不同:commitSetToBind 快照前 vs eval schema 后),未来新增校验步骤易漏|抽 commitSetToBind 复用(补 snapshotLabel 参数对齐)|selftest:eval 整体替换 ×protectedCtx/LEAF_BIND 与 write(set) 行为对齐断言
P2|flow-divergence|src/core/tools/dataOps.ts:739、785|interceptors.read/write 只守卫高层 read/write,advanced 模式下 set_data/edit_data/delete_data/draft_commit/eval 全部绕过|opts.interceptors 仅在 readSlot(739)与 writeSlot(785)消费;simple 默认底层工具被 SIMPLE_HIDDEN(112)隐藏无绕过面,toolMode:'advanced' 全暴露→「拦截器转换/审计/拒绝」承诺在 advanced 不成立|文档明示拦截器仅守 read/write,或底层写路径统一接 interceptor|e2e:advanced 模式 set_data 绕过拦截器行为断言(明示化)
P2|performance|src/core/tools/dataOps.ts:447-448、868-869|hashValue 对整体 bind 全量 safeStringify+cyrb53,且多处相邻两行重复计算(H3 证实,A3 惰性 hash 未做)|editData:447 lastReadHash=hashValue(bindRef) 与 448 消息内再算一次;writeSlot edit:868-869 同样双算;eval 645/658/685 各一次;每次 O(bind 全量序列化),几百 KB bind 每次写 ≥2 次全量 stringify|单次计算复用变量;A3 惰性/增量 hash 重启评估|selftest 基准:写路径 hashValue 调用计数断言
P3|doc-drift|CLAUDE.md「数据槽操作」interceptors 段|「interceptors.write 补充的不可见字段…写回 bind,不丢失」与实现相反|实现自 2.15 起安全收紧:未声明字段一律丢弃(sec-21.ts:376-392 两条断言显式验证拦截器补充字段被挡,注释说明 2.15.0 曾当修复实为白名单绕过口子已收窄);zod4 safeParse strip 已实测确认|CLAUDE.md 该句改为「未声明字段(含拦截器补充)被丢弃,声明字段保留」|无需补测试(已有)
P3|maintainability|src/core/tools/dataOps.ts:350|ConflictInfo.snapshotId 恒硬编码 0,无消费方|handleConflict 传 snapshotId:0;components/ 与 createChatSdk 内无 snapshotId 渲染消费(grep 证实)|删字段或填真实最近快照 id|无
P3|correctness|src/core/tools/dataOps.ts:708-724|read 多路径 jsonPaths 模式缺 isUnsafePath 检查(单路径 726 有)|白名单模式 '__proto__' 降级为 PATH_DENIED(错误码不准);开放模式靠 getByPath 内部守卫返 undefined,无安全洞但错误码契约不一致|多路径分支逐项补 isUnsafePath→PATH_UNSAFE|selftest:jsonPaths 含 __proto__ 断言 PATH_UNSAFE
P3|maintainability|src/core/tools/schemaUtils.ts:14-28|getSchemaTopKeys 不复用 unwrapSchema,自写解包循环且仅认 innerType 链|与 isPathAllowed/getSchemaAtPath 的 unwrapSchema(innerType/schema/getter 三路)逻辑重复;根级 lazy 返 null→白名单关闭是 CLAUDE.md 明示的向后兼容行为非 bug,但双份解包逻辑是漂移源|改用 unwrapSchema|selftest:optional/default 包裹的 ZodObject 顶层 key 提取断言
P3|crosstalk|src/core/tools/dataOps.ts:274-277 + conflictManager.ts:29-31|shareContext 双实例:新冲突自动按 keep_external 收口旧冲突,被收口方 LLM 无归因;autoLock 基线 lastReadHash 跨实例共享、并发 read 完成序不可重现|conflictManager 覆盖前自动收口防永挂(正确);被自动收口方只看到「已保留外部修改」不知曾发生冲突;lastReadHash 共享问题代码注释已自认|keep_external 收口消息补「因新冲突介入自动收口」归因;文档化并发写建议显式 expectedHash|e2e:shareContext 双实例连续冲突断言旧冲突收口语义

## H4 结论:证伪(enforce 已覆盖 eval 子树模式)

证据链:eval 子树 transform 经 applyPatchesToBind 落地(dataOps.ts:642 显式传 protectedCtx)→ applyPatchesToBind 在 patches 应用后、schema.safeParse 前调 enforcePatches(dataOps.ts:219-222)→ resources.ts:366-403(C3 remove 拒绝 + normalizeAndCheck clone-vs-bind 比对)。另核实 eval patches 模式同经 applyPatchesToBind(655)、eval 整体替换独立调 enforceSet(664-668);commitSetToBind/applyPatchesToBind 全部 7 处写调用点(405/443/642/655/864/877/990)均传 protectedCtx 无一遗漏。

附带核实(F8 其余项):D1 自愈触发点完整(写侧 expandHandle resources.ts:272-277 + 读侧懒注册 237/245 以 bind 现值为准;setData 显式清池 dataOps.ts:288;switchSession/checkpoint 池与 bind 同源恢复);C1 回显(resources.ts:299-303)/C2 patches[i] 定位(396-401)/C3 remove·delete 拒含 ancestor 关系(373-395 + dataOps.ts:478-482/834-838)均落实;占位符出现在 jsonPath 参数(而非值)时白名单模式 PATH_DENIED、开放模式仅创建无害怪键,无洞;freeze 无 vfs 工作 ✓(dataOps.ts:266-270),verbatim 无 vfs 降级为真值暴露但写侧仍 deepEqual 拦截(resources.ts:237);不传 expectedHash 直写兼容 ✓(dataOps.ts:339)。

## 一句话总评

写链单一真相源基本成立(set 类 3 路共用 commitSetToBind、edit 类 4 路共用 applyPatchesToBind、enforce 全覆盖含 eval 子树——H4 证伪),冲突闭环三处收口(abort/switchSession/unmount)已核实;真实风险集中在三处:整体/根级读浅投影泄露嵌套未声明字段(P1,白名单护城河唯一破口)、resetSession 漏冲突收口(P1,生命周期唯一缺口)、非法路径/非对象 merge 静默成功的反馈失真家族(P1+P2,误导 LLM 自纠)。
