# 待办问题清单(当前开放项)

> 本清单只保留**仍未落地**的问题(含决策方向)。已完成 / 已砍 / 已闭环项见文末「已闭环」索引,详细原文与评估在 `git log` 历史与 `CHANGELOG.md` 中,不在本文件维护。
> 总原则:**护城河是 schema 强约束 + 浏览器原生 + 自研轻量。继续往里塞 LangGraph / 通用代码生成 / 多模式原生编排反而稀释它。聚焦把现有能力(schema 自动白名单、乐观锁、headless)文档写清、bug 修净,比堆十个新选项值钱。**

---

## 当前开放项

### #12 流程编排插件(轻量中间件)
- **问题**:流程编排能否实现?最好是插件方式,用户可自定义流程编排规则(含 crewAI 流程编排)。
- **决策**:🔄 **轻量版**编排中间件可做(`createOrchestratorMiddleware({ mode:'plan-execute'|'pre-act' })`,靠中间件钩子 + 子 agent 组合,不改 createAgent 核心,覆盖 ~70% 编排需求);❌ **重量版**(3.0,createAgent 原生支持非 ReAct 循环)**砍**——重构 stream 循环高风险低收益。
- **现状(2026-08-03)**:❌ 未做 —— 无 `createOrchestrator*`;plan-execute 仍靠 subagent 组合模拟(planner-demo)。设想详见 `doc/archive/roadmap.md` #12。

### #13 GitHub Issues 模板 + 反馈渠道
- **问题**:如何配置免费官网?方便用户快速上手。最好能有反馈意见渠道,收集用户使用中的问题、建议。
- **决策**:🔄 只做 **GitHub Issues 模板**(bug/feature/question 三种)+ README 底部「反馈」链接(低成本高价值);❌ vitepress 单页官网 + 在线 demo **砍**(小 SDK 过度投入)。
- **现状(2026-08-03)**:❌ 未做 —— `.github/` 仅 ci.yml 无 ISSUE_TEMPLATE;README 无反馈链接。官网砍(正确)。

### #16 高级多 agent 架构(supervisor 文档示例)
- **问题**:高级多 agent 架构 sdk 设计?Event-driven / Graph-based / Supervisor / Network。
- **决策**:🔄 **Supervisor 模式**零新增,复用现有预声明子 agent(`subagents:[]`),只补文档示例 + demo;❓ Network / Event-driven / Graph-based 需新核心模块(事件总线/图状态机),按真实需求再定,别预先规划。
- **现状(2026-08-03)**:🔄 部分 —— README 已文档化 `subagents:[]`(planner-reflector-executor 固定角色示例),等价 supervisor 委派;无专门 supervisor demo。

### #20 记忆分类型分层管理(低优先,搁置)
- **问题**:记忆是不是可以分类型、分层管理?例如:工具信息 / 系统提示词 / 用户输入 / AI 输出 / 错误信息;某条上下文是否压缩过;临时恢复的上下文是否需要压缩、何时卸载。
- **决策**:❓ 中等工程(改 AgentMessage 类型 + summarization + trimMemoryMessages)。当前压缩后原文不可恢复;要做「分层保留 + 压缩可恢复」涉及核心消息模型改动,**优先级低于修 bug / 文档**。
- **现状(2026-08-03)**:❌ 未做(符合搁置决策)—— summarization 无分层。

### #21 纯代码组件逃生舱 + code-writer skill(方向已定,零落地)
- **问题**:纯前端页面代码编写 agent 的设计实现 —— 只生成前端代码 html + js + css 内联完整页面。
- **决策**:🔄 原「整页 HTML 生成」**砍**,改为「**纯代码组件逃生舱 + code-writer skill**」:
  1. **customCode 作为页面 schema 第 13 种 module type**,与 heading/button 同构:`props.code`(内联 HTML+CSS+JS,自包含)。agent 用 `write` 整体写,继承 schema 校验 + 快照回退 + 拦截器。渲染 `<iframe srcdoc sandbox="allow-scripts">`(无 allow-same-origin 隔离 parent/cookie)。`interceptors.write` 静态危险调用扫描(`eval`/`document.cookie`/`window.parent`)。**SDK 永不 eval 用户代码**。〔注(2026-08):interceptors 已于 3.31 移除,危险扫描若落地改走自定义 middleware/宿主侧;实际代码组件走了 createHtmlSubagent code-as-data-asset 路线〕
  2. **用 `code-writer` skill 注入规范,不用 subagent**(代码生成是「一次性产出字符串」,主 agent 直接写最省)。主体仍是结构化内置组件(90%),customCode 只占逃生舱(10%)。
  3. 强依赖模型能力(#15):systemPrompt 点明用强模型。
- **现状(2026-08-03)**:❌ 未做 —— complex-demo 无 customCode type、无 code-writer skill;方向已定(逃生舱),零落地。

---

## 已闭环(2026-08-03 清理,不再维护原文)

| # | 决策 | 落地情况 |
|---|---|---|
| #1 | 大上下文 LLM → 小上下文 LLM 处理 | ✅ `sdk.setLlm` 运行时切换 + `onLlmChange` 重解析 modelCaps |
| #8 | `extraBody` 透传 | ✅ `modelKwargs` |
| #10 | 修 `contextOptions:false` bug | ✅ 类型支持 `false` |
| #11 | README 简化 + examples | ✅ `minimal-demo` + `headless-demo` 入 examples/ |
| #14 | 模式 SDK 砍 | ✅ 砍对(无 createPlanExecuteSdk,与 #12 重复) |
| #15 | 推荐高级 LLM | ✅ 默认模型统一 `deepseek-v4-flash` |
| #17 | subpath + 按需引入文档 | ✅ exports `./storage`/`./query`/`./llm`(2.14.0) |
| #18 | 长任务完善 | ✅ 超额落地:automation(`tokenBudget`/`timeBudgetMs`/`maxAutoRetries`/断点续跑/`sdk.batch`) |
| 2.11.0 精简集 | #10/#8/#11/#17 四件 | ✅ 全部落地 |
| 新增问题 1 | 大→小 LLM 切换 | ✅ 已做(`setLlm` + modelCaps) |

> 各已闭环项的详细评估、决策依据见 `git log`(2026-08-03 之前的 `doc/问题.md` 版本)与 `CHANGELOG.md`。
